specVersion: "v1"
serviceId: "firebase-learning"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "firebase-learning-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./example"]
